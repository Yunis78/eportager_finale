{% extends 'base.html.twig' %}

{% block title %}CommandeProduct{% endblock %}

{% block body %}
    <h1>CommandeProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_product.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ commande_product.quantity }}</td>
            </tr>
            <tr>
                <th>UnitPrice</th>
                <td>{{ commande_product.unitPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_product_index') }}">back to list</a>

    <a href="{{ path('commande_product_edit', {'id': commande_product.id}) }}">edit</a>

    {{ include('components/pages/commande_product/_delete_form.html.twig') }}
{% endblock %}
