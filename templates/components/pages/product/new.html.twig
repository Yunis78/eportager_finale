{% extends 'base.html.twig' %}

{% block title %}New Product{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex justify-content-between">
        <h1>Create new Product</h1>
        <p><a class="btn btn-primary" href="{{ path('product_index') }}">back to list</a></p>
    </div>

    {{ include('components/pages/product/_form.html.twig') }}

    
</div>
{% endblock %}
