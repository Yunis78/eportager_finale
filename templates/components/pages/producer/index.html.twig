{% extends 'base.html.twig' %}

{% block title %}Producer index{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mb-4">Producer index</h1>

    <table class="table table-sm table-hover table-end-fit">
        <thead class="table-active">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Siret</th>
                <th>Addresszip</th>
                <th>Villename</th>
                <th>Phone</th>
                <th>Active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for producer in producers %}
            <tr>
                <th scope="row">{{ producer.id }}</th>
                <td>{{ producer.name }}</td>
                <td>{{ producer.description }}</td>
                <td>{{ producer.addresszip }}</td>
                <td>{{ producer.villename }}</td>
                <td>{{ producer.siret }}</td>
                <td>{{ producer.phone }}</td>
                <td>{{ producer.active ? 'Yes' : 'No' }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('producer_show', {'id': producer.id}) }}">show</a>
                    <a class="btn btnorange" href="{{ path('producer_edit', {'id': producer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <p><a class="btn btn-success" href="{{ path('producer_new') }}">Create new</a></p>
</div>
{% endblock %}
